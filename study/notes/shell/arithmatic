数值计算
========================================
1.计算的数值的五种方法：
	((i++))
	let i++
	i=$(expr $i + 1)
	i=$(echo $i+1|bc)
	i=$(echo $i 1|awk '{printf $1+$2}')

2.time命令用来统计命令的执行时间，这部分时间包括总的运行时间，用户空间执行时间，内核空间执行时间，它通过ptrace系统调用实现。

3.取模使用符号%，而求幂只有bc使用的符号为^，其他命令使用的符号均为**。
